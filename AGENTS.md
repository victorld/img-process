# 仓库约定

- git commit 的提交描述必须使用中文。
- 项目默认通过 Docker 部署和运行，除非用户明确说明，否则不要假设直接本机进程是最终生效环境。
- 任何会影响前端、后端或配置的修改完成后，都必须让 Docker 中的实际服务立即生效，并验证对外访问已经切到新版本。
- 对本仓库做变更时，默认使用 `docker compose up -d --build app`；如果因为构建耗时或环境限制采用临时同步方案，也必须补充完成容器重启和生效验证。
