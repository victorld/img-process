# img-process 分支变更时间线

## 范围说明

- 当前分支：`codex-dev-0409`
- 对比基线：`master`
- 分叉点：`9c2ae5d21b7c9496b93bd7c673c68dd14b0b4e00`
- 统计范围：`9c2ae5d..HEAD`
- 统计口径：只记录有效代码、配置、接口、部署、数据库改动；`.playwright-cli` 等调试产物不纳入主时间线

## 更新策略

- 这份文件记录的是当前工作分支相对 `master` 的变更时间线，后续新增有效提交时必须继续维护。
- 新增内容一律按提交时间顺序追加，不按模块重排已有内容。
- 每个提交节点固定用大纲层级拆成两部分：
  - `#### 功能变化`
  - `#### 数据库变化`
- `#### 数据库变化` 必须优先补充字段级变更，涉及表结构时至少写清楚：`表名`、`字段名`、`动作（新增/修改/删除）`，以及类型、默认值、可空、索引/唯一约束、回填或兼容性影响中的关键信息。
- 如果没有字段变化，但有数据库初始化、连接串、库名、主机端口、账号或部署连接方式变更，必须明确写成“数据库连接配置变化”或“数据库初始化行为变化”。
- 某次提交没有数据库改动时，也必须明确写“本次没有新增表、字段或数据库连接配置变化”。
- 不再单独维护文末“数据库变化”汇总章节，数据库相关变化只保留在对应提交节点下。
- 调试产物、临时日志、页面抓取结果等内容不进入主时间线，只允许在附注中说明被排除。
- 推荐写法示例：
  - `- 表 \`scan_job\`：新增字段 \`status\`（varchar(32)，默认 \`pending\`，非空，普通索引）。`
  - `- 表 \`scan_action_item\`：修改字段 \`metadata\`（text -> json，历史数据无需回填，接口返回改为结构化读取）。`
  - `- 本次没有新增表、字段或数据库连接配置变化。`

## 时间线

### 2026-04-11 23:53 +0800 | `3e3ce0b` | 修复没有建 `img` 库报错的问题

#### 功能变化

- Web、扫描、GIS、RPC 等入口统一改为接收数据库初始化错误，启动失败时可以明确退出，而不是在运行期才暴露问题。
- 扫描流程补充异常路径处理，减少数据库未就绪时的连带报错。

#### 数据库变化

- 数据库初始化从“直接连接目标库”改为“目标库不存在时自动创建后再连接”，避免首次部署因 `Unknown database` 直接启动失败。
- `plugin/orm/gorm_mysql.go` 增加建库逻辑与脱敏连接日志，属于数据库初始化行为变化。

### 2026-04-12 00:38 +0800 | `a6a46f4` | 修复扫描主流程 P0 问题并校正 `is_complete` 状态

#### 功能变化

- 扫描运行时状态收敛到 `Scanner` 实例，消除跨任务共享可变状态带来的并发污染。
- EXIF、GIS、图片缓存链路改成实例级依赖，扫描主流程从“依赖包级全局状态”切换为“显式注入”。
- `exiftool` 调用由 shell 拼接改为参数化执行，降低命令注入和环境依赖风险。
- `is_complete` 语义被校正，EXIF EOF 不再把整次扫描误判为失败，而是只计入文件级错误。

#### 数据库变化

- 本次没有新增表、字段或数据库连接配置变化。

### 2026-04-12 01:22 +0800 | `3287f92` | 补充 `exiftool` 缺失时的明确安装提示

#### 功能变化

- 扫描启动时增加 `exiftool` 可用性检查。
- 系统未安装 `exiftool` 时输出中文提示，并明确说明会自动回退到 Go EXIF 解析。
- 运行行为从“静默退化”改为“有感知退化”，方便排查拍摄时间和 GPS 识别不完整的问题。

#### 数据库变化

- 本次没有新增表、字段或数据库连接配置变化。

### 2026-04-12 09:22 +0800 | `85dae93` | 优化扫描服务的安全性稳定性与接口行为

#### 功能变化

- 配置初始化改为显式返回错误，启动日志做脱敏处理。
- GIS 请求链路增加超时、状态码校验与更安全的 JSON 解析，降低外部依赖不稳定带来的扫描中断。
- 文件移动与写文件逻辑改为返回真实错误，跨盘移动支持 `copy + delete` 回退。
- 扫描 API 补充真实 HTTP 状态码，并新增 `POST/DELETE` 入口，同时保留旧 `GET` 兼容行为。

#### 数据库变化

- 数据库连接日志、SQL 调试开关、DAO 错误透传方式被调整，但本次没有新增表结构。

### 2026-04-12 11:18 +0800 | `eb24082` | 重构扫描链路并固化中文提交约定

#### 功能变化

- 项目启动流程抽到 `bootstrap`，主程序入口统一改为通过初始化阶段装配配置、数据库和日志。
- 扫描决策逻辑拆分出 `scan_decision`，把“日期判断、移动/改名/改时间决策”从主流程中解耦。
- 扫描参数模型调整，运行时配置读取方式和默认值回填逻辑一并收敛。
- 仓库约定被显式固化，要求提交信息使用中文。

#### 数据库变化

- 本次没有新增表、字段或数据库连接配置变化。

### 2026-04-13 05:52 +0800 | `836e1df` | 实现扫描任务 Web 管理台与调度能力

#### 功能变化

- 新增 Web 管理台，支持任务列表、任务详情、实时事件、计划任务、系统设置等页面。
- 后端新增任务、动作、事件、计划任务、会话鉴权等完整接口，扫描任务从单次命令式执行扩展为可追踪、可调度的运行模型。
- 运行时新增任务调度器、事件记录器、动作记录器，扫描过程可以持久化为任务历史。

#### 数据库变化

- 新增 `scan_job`、`scan_action_item`、`scan_event`、`scan_schedule` 四张核心表，并在 `middleware/register_table.go` 中加入建表注册。

### 2026-04-13 06:23 +0800 | `089a059` | 修复 Docker 启动与计划任务运行问题

#### 功能变化

- Docker 环境新增独立配置文件 `docker/config.docker.yaml`，把容器内默认扫描路径、端口、数据库地址固定下来。
- `register_table` 从“吞掉错误继续启动”改为“返回错误并中止启动”，避免计划任务依赖的表未建好却继续运行。
- Web 服务启动流程补上建表错误处理，Docker 启动失败时可以更快暴露根因。

#### 数据库变化

- `model/common.go` 为公共主键和时间字段补齐 JSON 输出约定，影响所有表的接口序列化表现。
- `scan_action_item` 模型字段细节有小幅修正。
- Docker 数据库连接配置首次独立落盘到 `docker/config.docker.yaml`。

### 2026-04-13 10:59 +0800 | `aed0dee` | 修复扫描路径校验并增加任务实时日志

#### 功能变化

- 任务运行时补充 `scan_job_log` 日志流，详情页可以查看阶段日志与负载信息。
- Web 端任务详情页从“只看事件”扩展到“事件 + 日志 + 任务状态”联动展示。
- 扫描路径校验逻辑修复，计划任务和手动任务的路径检查更严格，减少无效任务入队。

#### 数据库变化

- 新增 `scan_job_log` 表，并在建表注册中加入日志表。
- 运行时开始将结构化日志持久化到数据库。

### 2026-04-14 00:49 +0800 | `faa3e4d` | 重构扫描详情动作明细

#### 功能变化

- 任务详情页待执行动作从单表升级为“删除、移动、修改时间、重复项、重命名”五类视图，并增加总待操作数。
- 动作明细接口扩展为结构化返回，支持分项计数、重复项 A/B 成对展示、图片预览接口。
- 扫描阶段补齐动作元数据写入，前端不再依赖原始字符串拼接推断文件名、日期、目标位置。
- 仓库文档补充 Docker 为默认交付环境，并要求代码修改后立即让容器内服务生效。

#### 数据库变化

- `scan_action_item` 的元数据承载能力被实质扩展，动作明细开始稳定记录展示所需的结构化信息。

### 2026-04-14 09:12 +0800 | `34cbba0` | 更新 Docker 配置以复用外部 MySQL 并调整默认登录

#### 功能变化

- Docker Compose 不再在当前项目里自带数据库编排，而是直接复用外部公共 MySQL。
- 默认登录配置和 Docker 环境配置同步调整，容器启动路径更贴近实际部署环境。
- 本地与容器的配置边界进一步明确，默认运行方式从“项目自带全套编排”转向“服务复用外部基础设施”。

#### 数据库变化

- `docker/config.docker.yaml` 中的数据库主机、端口、登录配置被调整，属于数据库部署连接方式变化，不涉及表结构新增。

### 2026-04-14 23:37 +0800 | `ae7f20c` | 扫描管理台中文化收口

#### 功能变化

- Web 管理台中的任务状态、任务来源、当前阶段、计划模式、最近状态、日志级别等枚举展示统一改为中文，列表、详情、仪表盘、计划任务、设置页不再直接透出英文原值。
- 扫描详情页的事件流与运行日志中文案同步收口，用户可见的阶段变更、进度更新、任务完成、任务失败、重复文件删除等标题和消息统一改为中文。
- 计划任务与 Cron 相关的前后端提示文案改为中文，同时保留接口参数、查询条件、数据库枚举值继续使用英文，避免影响现有兼容性。

#### 数据库变化

- 本次没有新增表、字段或数据库连接配置变化。

## 附注

- 分支历史中存在 `.playwright-cli` 调试日志与页面抓取产物，但它们不属于功能、接口、部署或数据库层面的有效改动，本文件未把这些内容写入主时间线。

### 2026-04-14 14:20 +0800 | `WORKTREE` | 详情页增加删除类单条删除与全部删除

#### 功能变化

- 扫描详情页的“删除”待执行列表新增逐条删除按钮，点击后会立即执行当前删除动作并回写执行状态。
- 扫描详情页新增“全部删除”按钮，可一次性执行当前任务下全部待删除项，执行后刷新动作列表、事件流与任务信息。
- 后端新增删除类动作执行接口，并将删除类查询统一覆盖普通文件删除与空目录删除，保证页签数量、列表内容与批量执行范围一致。

#### 数据库变化

- 本次没有新增表、字段或数据库连接配置变化。

### 2026-04-14 23:43:40 +0800 | `uncommitted` | 统一管理台时间展示格式

#### 功能变化

- Web 管理台新增统一时间格式化工具，所有用户可见时间按 `Asia/Shanghai` 展示为 `YYYY-MM-DD HH:mm:ss`，不再直接显示 RFC3339 中的 `T` 和时区尾缀。
- 扫描历史页、扫描详情页、动作明细、事件流、运行日志、计划任务页的时间字段统一切到同一展示入口，空值显示为 `-`，非法值保留原始字符串。
- 本次仅调整前端展示层，后端 Web API 时间字段返回结构和类型保持不变。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:00:00 +0800 | `uncommitted` | 调整移动待执行列表的路径展示口径

#### 功能变化

- 扫描详情页“移动”待执行列表中的“当前位置”和“移动位置”改为仅展示父目录，不再重复携带文件名。
- 新增前端路径展示 helper，统一处理普通路径、根目录文件路径和无分隔符异常字符串，保证只影响显示层。
- 本次仅调整“移动”tab 的展示口径，历史动作表及其他待执行类型仍保持完整路径显示。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:20:00 +0800 | `uncommitted` | 取消管理台表格内部滚动条

#### 功能变化

- Web 管理台表格新增统一无滚动样式，所有 `antd Table` 默认改为固定布局和单元格自动换行，不再依赖表格内部横向或纵向滚动条。
- 扫描历史页与计划任务页的列宽和操作区布局被压缩，长任务标识、Cron、时区和按钮组会在单元格内换行，避免撑出滚动条。
- 扫描详情页的待执行、已执行和错误动作表去掉横向滚动与固定操作列，图片列和时间列收紧后继续保留表格形态展示。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:15:00 +0800 | `uncommitted` | 修改时间页签改为展示文件修改时间

#### 功能变化

- 扫描详情页“修改时间”待执行列表中，原“照片拍摄日志（原始）”列改为“文件修改时间”，展示值切换为文件实际 `modifyDate`。
- 后端动作明细元数据与详情接口补充透出 `modifyDate` 字段，前端不再复用 EXIF 原始拍摄时间字段来占位显示。
- 对历史动作记录增加兼容回退：若旧元数据里没有 `modifyDate`，详情接口会从源文件当前修改时间补齐展示值。
- 本次调整仅影响“修改时间”页签展示口径，其他待执行类型仍保持原有拍摄时间字段展示。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:16:42 +0800 | `uncommitted` | 调整重复项页签为三列双栏对照

#### 功能变化

- 扫描详情页“重复项”待执行列表由原先 A/B 拆开的 6 列收敛为“照片”“照片文件名”“照片位置”3 列，表头层级更简洁。
- 每个重复项单元格内部改为 A、B 左右双栏对照展示，同一属性下可直接横向比较两张照片、文件名和位置。
- 新增重复项双栏卡片样式，并在窄屏下自动切换为单列堆叠，避免横向挤压导致可读性下降。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:17:23 +0800 | `uncommitted` | 调整重命名待执行列表列展示

#### 功能变化

- 扫描详情页“重命名”待执行列表移除“照片拍摄日期”和“照片拍摄日志（原始）”两列，减少与当前重命名决策无关的冗余信息。
- 扫描详情页“重命名”待执行列表中的“当前位置”改为仅展示父目录，不再重复携带文件名，和“移动”页签保持一致的路径展示口径。
- 本次仅调整前端展示层，后端动作明细字段和接口返回结构保持不变。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:53:35 +0800 | `uncommitted` | 重复项空列表补充未开启检测提示

#### 功能变化

- 扫描详情页“重复项”页签在当前任务未开启 `md5Show` 时，新增明确提示，说明本次扫描不会产出重复项数据。
- “重复项”页签的空状态文案按任务配置区分：未开启重复检测时展示原因提示，已开启但无结果时仍展示通用“暂无待执行动作”。
- 本次调整仅影响详情页前端提示，不改变重复项查询接口、扫描逻辑和数据库数据。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:32:44 +0800 | `uncommitted` | 修复详情页待执行统计回退口径

#### 功能变化

- 扫描详情页待执行统计从 `job.summary` 回退取值时，兼容后端当前返回的首字母大写键名（如 `DeleteFileCnt`、`MoveFileCnt`、`RenameFileCnt`），避免统计卡片和页签数量错误显示为 0。
- 当动作明细接口尚未返回、查询失败或页面首次渲染走回退逻辑时，前端会继续展示与任务摘要一致的待删除、待移动、待修改时间、待重命名、待重复项数量。
- 本次仅调整前端展示回退逻辑，不修改动作明细接口、任务详情接口和数据库字段。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:40:00 +0800 | `uncommitted` | 调整修改时间待执行列表列顺序

#### 功能变化

- 扫描详情页“修改时间”待执行列表中，“照片拍摄日期”调整到倒数第二列，位于“文件修改时间”之后、“最小日期”之前。
- 本次仅调整前端列顺序，不修改动作明细字段、接口返回结构和后端处理逻辑。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:41:00 +0800 | `uncommitted` | 删除待执行列表增加原因列

#### 功能变化

- 扫描详情页“删除”待执行列表新增“原因”列，和已执行动作表保持一致，可直接看到每条删除候选的触发原因。
- 删除原因展示增加前端细化兜底：对历史数据里泛化的“文件名命中删除规则”，会根据文件名进一步展示为“文件名以 . 开头”“文件名以 IMG_E 开头”“文件名以 _.pic.jpg 结尾”“文件名以 nas_downloading 结尾”等具体规则。
- 空目录删除候选统一展示为“空目录”；当删除候选无法从文件名规则识别时，前端会回退展示为“文件大小为 0”，覆盖零字节文件场景。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 01:05:00 +0800 | `uncommitted` | 修复移动目标路径被重命名目标覆盖

#### 功能变化

- 扫描决策阶段将“移动目标路径”和“重命名目标路径”拆分为独立字段，不再共用同一个内部 `targetPhoto` 状态，避免同一文件同时需要移动和重命名时相互覆盖。
- 移动候选动作的 `targetPath` 固定写入最小日期目录下的目标文件；重命名候选动作的 `targetPath` 单独记录最终重命名落点，详情页“移动位置”会回到最小日期目录。
- 执行链路补充“先移动再重命名”的路径衔接：移动成功后，后续重命名会以移动后的实际文件路径作为源继续执行，避免重命名阶段仍指向旧目录。
- 补充决策、动作落库和执行链路测试，覆盖 move 与 rename 同时发生时的目标路径和执行顺序。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 00:59:06 +0800 | `uncommitted` | 重复项 AB 对照改为上下排布

#### 功能变化

- 扫描详情页“重复项”列表中，每个属性列内的 A/B 对照由左右双栏改为上下两行，A 在上、B 在下，便于纵向逐项比较。
- 照片、照片文件名、照片位置三列统一沿用同一纵向排版规则，不再因为桌面宽度充足而自动横向并排。
- 本次仅调整前端展示样式，不改变重复项数据结构、查询接口和预览图片加载逻辑。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 01:05:09 +0800 | `uncommitted` | 重复项改为 AB 独立列并按子行对齐

#### 功能变化

- 扫描详情页“重复项”列表新增独立 `AB` 列，后续“照片”“照片文件名”“照片位置”三列不再重复显示 A/B 标签。
- 每组重复项改为两条连续子行展示：A 一行、B 一行，同组在同一条表格横向基线内对齐，不再出现列间高低参差。
- 重复项行样式补充组内边框与 `AB` 标签样式，成对数据的阅读路径更清晰。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 01:07:44 +0800 | `uncommitted` | 重复项位置列仅展示父目录

#### 功能变化

- 扫描详情页“重复项”列表中的“照片位置”改为仅展示父目录，不再重复携带文件名。
- “照片文件名”与“照片位置”两列职责拆清：文件名只在文件名列展示，位置列只保留目录路径，阅读负担更低。
- 本次调整仅影响重复项列表前端展示，不改变重复项数据查询和图片预览逻辑。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 11:39:29 +0800 | `uncommitted` | 重复项增加建议删除与单条操作

#### 功能变化

- 扫描详情页“重复项”列表新增“建议删除”和“操作”两列：A 行固定显示红色“是”，A/B 两行都可单独点击“删除”。
- 重复项页签顶部新增“按建议删除所有照片”按钮，复用现有批量重复删除逻辑，仅执行当前建议删除侧的照片。
- 后端新增重复项单条删除接口，支持按 `A/B` 指定删除建议删除图或保留图；执行后会把实际删除侧、实际删除路径和原建议删除路径写回动作元数据，便于审计追踪。
- 批量重复删除改为只处理当前仍处于待执行状态的重复项，避免对已经单条处理过的记录重复执行。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 01:01:17 +0800 | `uncommitted` | 移动待执行列表改为展示文件修改时间

#### 功能变化

- 扫描详情页“移动”待执行列表中，原“照片拍摄日志（原始）”列改为“文件修改时间”，展示值切换为详情数据里的 `modifyDate`。
- 本次仅调整“移动”页签展示字段，不修改动作明细接口结构、后端扫描决策逻辑和其他待执行列表列定义；旧记录仍沿用详情接口现有的修改时间补齐逻辑。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 01:30:00 +0800 | `uncommitted` | 动作明细接口补充分组计数并打通重命名结果可见性

#### 功能变化

- `GET /api/jobs/:id/action-items` 在保留原有 `counts` 待执行统计的同时，新增 `groupedCounts.pending|executed|error`，用于区分待执行、已执行、失败三类动作计数。
- 扫描详情页的动作类型筛选从“仅待执行动作可按类型查看”扩展为“待执行 / 已执行 / 错误”三类页签都可按删除、移动、修改时间、重复项、重命名分类查看。
- 当“待执行动作 -> 重命名”为空但任务已有已执行或失败的重命名记录时，页面会直接提示并提供跳转按钮，避免用户误判为本次扫描没有出现 rename。
- 已执行动作和错误页中的重命名列表沿用重命名专属列展示原文件名、新文件名、当前位置，并追加状态、执行时间、错误信息；其他动作类型也补充统一的执行结果列。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 12:00:14 +0800 | `uncommitted` | 修改时间页签增加拍摄时间变更操作

#### 功能变化

- 扫描详情页“修改时间”待执行列表新增“操作”列，每条记录都可点击“变更”，把当前文件的 EXIF 拍摄时间写成“最小日期 00:00:00”。
- 前端新增 `POST /api/jobs/:id/action-items/:itemId/modify-shoot-time` 调用；执行成功后会刷新任务详情、动作列表和事件流，失败时直接透出后端错误。
- 后端新增修改拍摄时间执行链路：校验 `modify_time` 待执行动作、读取 `targetDate/minDate` 作为目标日期、优先调用 `exiftool` 写入 `DateTimeOriginal`，命令缺失时自动回退到 Perl `Image::ExifTool` 模块，并把实际写入值回填到动作元数据与执行记录中。
- 单条拍摄时间变更成功后，会把同文件当前任务下旧的待执行 `move`、`rename`、`modify_time` 候选统一标记为“已跳过（候选已重算）”，再复用现有单文件扫描决策逻辑重建最新候选，避免页面继续展示过期动作。
- 新增 API 与服务层单测，覆盖修改拍摄时间接口成功/参数错误分支，以及目标拍摄时间格式化、目标日期选择和重算跳过标记逻辑。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 12:20:00 +0800 | `uncommitted` | 移除扫描详情页顶部重复文件删除按钮

#### 功能变化

- 扫描详情页标题右侧的“执行重复文件删除”按钮已移除，避免和“重复项”页签内现有的批量操作入口形成重复。
- 重复项页签中的“按建议删除所有照片”按钮保持不变，重复项批量处理能力仍通过页签内入口使用。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 12:35:00 +0800 | `uncommitted` | 重命名页签增加单条变更操作

#### 功能变化

- 扫描详情页“重命名”待执行列表新增“操作”列，每条记录都可点击“变更”，按当前建议文件名立即执行单条重命名。
- 前端新增 `POST /api/jobs/:id/action-items/:itemId/rename` 调用；执行成功后会刷新任务详情、动作列表和事件流，失败时直接透出后端错误。
- 后端新增单条 rename 执行链路：校验 `rename` 待执行动作、读取目标路径执行文件移动/重命名，并把目标文件名写回动作执行元数据与事件日志。
- 单条重命名成功后，会把同文件当前任务下旧的待执行 `move`、`rename`、`modify_time` 候选统一标记为“已跳过（候选已重算）”，再基于重命名后的实际路径重建最新候选，避免页面继续展示旧路径上的过期动作。
- 新增 API 与服务层单测，覆盖重命名接口成功/参数错误分支，以及 rename 目标路径解析、重算跳过标记逻辑。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 16:50:18 +0800 | `uncommitted` | 移除扫描详情页冗余统计卡片

#### 功能变化

- 扫描详情页顶部总览中移除“总待操作数”和“重复项数量”两张统计卡片，只保留“已处理文件”和“总文件数”。
- 扫描详情页“待执行动作 / 已执行动作 / 错误”页签内移除顶部摘要卡行，不再重复展示总数、重命名数和删除类数量，页面直接进入动作类型页签与列表内容。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 16:55:00 +0800 | `uncommitted` | 移动页签增加单条位置变更操作

#### 功能变化

- 扫描详情页“移动”待执行列表新增“操作”列，每条记录都可点击“变更”，按当前建议目标目录立即执行移动。
- 前端新增 `POST /api/jobs/:id/action-items/:itemId/move` 调用；执行成功后会刷新任务详情、动作列表和事件流，失败时直接透出后端错误。
- 后端新增单条移动执行链路：校验 `move` 待执行动作、按动作元数据中的目标路径执行移动，并把执行结果回写到动作状态和运行事件。
- 单条移动成功后，会把同文件当前任务下旧的待执行 `move`、`rename`、`modify_time` 候选统一标记为“已跳过（候选已重算）”，再基于移动后的实际路径重建最新候选，避免后续操作继续指向旧目录。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 18:21:49 +0800 | `uncommitted` | 移除扫描详情页错误主页签

#### 功能变化

- 扫描详情页顶部主页签移除“错误”，页面入口调整为仅保留“待执行动作”“已执行动作”“实时事件”“运行日志”“统计”“原始参数”。
- 当“待执行动作 -> 重命名”为空但已有历史结果时，提示文案只保留跳转到“已执行动作”的入口，避免界面继续暴露已下线的失败页签。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 18:55:00 +0800 | `uncommitted` | 扫描详情主页签补充动作数量

#### 功能变化

- 扫描详情页顶部主页签中的“待执行动作”“已执行动作”文案改为带括号数量展示，分别显示当前待执行总数和已执行总数，便于在不切换页签时直接判断动作规模。
- 页签数量直接复用动作分组统计里的 `pending.total` 和 `executed.total`，随详情数据刷新实时更新，不新增额外接口请求。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 18:56:00 +0800 | `uncommitted` | 调整扫描详情页顶部统计口径

#### 功能变化

- 扫描详情页顶部左侧统计卡由“已处理文件”改为“总文件夹数”，展示任务汇总里的目录总数 `dirTotal`。
- 顶部右侧“总文件数”继续保留原有文件总数展示，不调整现有统计口径。
- 前端读取目录总数时补充兼容 `dirTotal` / `DirTotal` 两种键名，避免不同返回结构下统计卡回退为 0。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 19:01:45 +0800 | `uncommitted` | 调整扫描历史列表统计列

#### 功能变化

- 扫描历史列表移除“当前阶段”“已处理”“执行动作”三列，改为展示“总文件夹数”“总文件数”“待执行动作”“已执行动作”。
- `GET /api/jobs` 列表返回补充 `totalFolderCount`、`pendingActionCount`、`executedActionCount`，列表页不再依赖详情接口即可直接展示任务汇总与动作规模。
- 任务列表中的“总文件夹数”兼容从任务摘要 `dirTotal` / `DirTotal` 取值；“待执行动作”“已执行动作”来自动作明细表按任务聚合统计，避免历史任务列表出现固定 0 的假值。

#### 数据库变化

- 无数据库变化。

### 2026-04-15 19:11:45 +0800 | `uncommitted` | 补充调试产物与运行数据库忽略规则

#### 功能变化

- `.gitignore` 新增 `.playwright-cli/` 规则，忽略 Playwright CLI 生成的控制台日志和页面抓取结果，避免调试产物混入提交。
- `.gitignore` 新增 `output/*.db` 规则，忽略本地运行生成的 SQLite 数据库文件，避免环境态数据进入版本库。
- `.dockerignore` 同步排除 `.playwright-cli` 和 `output`，减少 Docker 构建上下文里的无关调试文件与运行数据。

#### 数据库变化

- 无数据库变化。
